# 安全政策

> **发布状态：** App `0.1.1b1` 是未发布的本地 Windows beta 候选版。本文件定义
> 候选版的受控安全报告边界，不提供公开仓库、公开 issue、security advisory 或响应
> 时间承诺。

## 报告范围

请报告可能导致以下问题的缺陷：

- 绕过 localhost、Host、Origin、session 或 CSRF 边界；
- 泄露 startup/session/CSRF token、源文件路径、论文内容、Agent payload 或其他受限数据；
- 绕过 Core compatibility、workspace 隔离、路径/ACL/reparse 检查或写入审批；
- 通过 PDF、解析文本、Agent 输出或交换数据扩大权限、执行不受控操作或破坏数据完整性；
- bootstrap、打包或日志处理导致依赖、凭据或本地隐私信息暴露。

## 私下报告

当前候选版尚未发布，因此不要通过公开 issue、公开 pull request、社交平台或公开聊天
披露安全问题。请使用维护者已经指定的受控协作渠道单独提交报告；项目尚未公开固定的
邮箱、报告 URL 或其他身份入口。若尚未获得 private reporting channel，请先保留最小化、
已脱敏的本地记录并在受控协作中请求报告入口，不要公开披露细节。

报告应尽量包含：

1. 候选版本、Windows 版本、CPython 版本和 Core pin；
2. 受影响的相对文件路径或功能边界，不要提供本机绝对路径；
3. 可重复的最小步骤、预期结果、实际结果和影响范围；
4. 必要的脱敏日志、错误类型和建议缓解措施；
5. 是否能在 synthetic fixture 上复现，以及是否需要特殊权限或配置。

不要附加真实 PDF、解析全文、Evidence quote、研究笔记、workspace export、凭据、token、
云端链接或包含个人/机构信息的日志。需要证明数据泄露时，只提供最小化的占位样本和
脱敏后的结构，不要把泄露内容复制进报告。

## 处理边界

维护者会在受控协作渠道中确认是否需要补充复现、修复或暂缓披露。当前候选版不承诺
响应时间、修复时间、公告时间或发布版本；未完成身份、发布和恢复责任确认前，不得
将一次本地修复、测试通过或候选包构建描述为公开安全修复。

安全报告之外的普通缺陷和使用问题请参阅 [`SUPPORT.md`](SUPPORT.md)。
